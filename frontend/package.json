{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i ./src/app.css -o ./dist/app.css",
    "build:js": "esbuild src/app.js --bundle --outfile=dist/app.js",
    "build": "npm run build:css && npm run build:js",
    "dev": "uv run fastapi run --reload"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "alpinejs": "^3.14.8",
    "franken-ui": "^2.0.0-internal.45",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17"
  }
}
